# -*- coding: utf-8 -*-P
from kivy.uix.screenmanager import Screen
from kivy.uix.label import Label
from kivy.metrics import dp
class Title(Screen):
    def on_pre_enter(self):
        self.clear_widgets()
        self.add_widget(Label(text='[ Neon Runner A / Day1 ]\nEnter/Space/Touch', size_hint=(1,1), font_size=dp(24)))

        # Day6:TODO （発展）タイトル画面に Best（ハイスコア）を表示する
        # ------------------------------------------------------------
        # ねらい：ゲーム開始前に「前回までの最高記録」が見えると、やる気が上がります。
        #
        # 実装例（設計の方向性）：
        # - ScoringService の best を読む
        # - もしくは Day6 で作る save/best_score.json を読み込んで表示する
        #
        # 注意：
        # - Title は “見せるだけ” にして、ゲームロジックは Play 側に寄せてもOK
        # - ここでは「表示用のラベルを追加する」だけでも十分な達成になります
        # ------------------------------------------------------------

    def on_key_down(self, key, keycode):
        # TODO: 'enter','numpadenter','spacebar' で play へ
        return False
    def on_touch_down(self,*_):
        self.manager.current='play'; return True
